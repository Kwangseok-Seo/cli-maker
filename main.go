package main

import "fmt"

// cli-maker: 여러 web API 를 하나의 CLI 로 다루기 위한 런타임 인터프리터.
// M0 골격 — 다음 단계(M1)에서 cobra 루트 명령으로 대체한다.
func main() {
	fmt.Println("cli-maker: 여러 web API 를 위한 CLI (개발 중 — M0 골격)")
}
