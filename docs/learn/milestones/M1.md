# M1 — Cobra 루트 명령 + 첫 외부 의존성

> 커밋: `feat: add cobra root command (M1)` · 로드맵: M1

## 한 일

`os.Args` 손파싱을 cobra 루트 명령으로 교체. 첫 외부 라이브러리(cobra)를 `go get` → import → `go mod tidy` 로 도입. 이어서 `greet` 하위 명령을 `AddCommand` 로 붙이고 `cobra.ExactArgs(1)` 로 인자를 검증 → panic 없이 친절한 에러. `--help` 자동 생성·트리 등재 확인. **M1 완료.**

## 이 단계에서 익힌 개념

[[structs]] · [[pointers]] · [[functions-as-values]] · [[go-modules]] · [[cobra]]

## 대표 함정

- [[go-modules]]: `gihub.com` 오타 → HTTPS 강등 거부 / import 전 `go mod tidy` → cobra 제거.

## 결정

[ADR-0001](../adr/0001-runtime-interpreter-architecture.md) — cobra 채택 맥락(런타임 인터프리터).
